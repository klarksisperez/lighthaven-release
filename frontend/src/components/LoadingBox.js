/* This is considered a buffer in switching from one page to another */

import React from 'react'

export default function LoadingBox() {
    return (
        <div className = "loading-container">
            Loading
        </div>
    );
}
